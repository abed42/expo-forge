# expo-forge

production-grade Turborepo template for Expo apps — clone, add keys, `eas submit`

## Quickstart

```sh
bun install
cp .env.example .env
# add keys to .env
cd apps/mobile && bunx expo run:ios
```
