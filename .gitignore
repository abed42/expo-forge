node_modules
.expo
ios/
android/
dist
# All env files are secrets except the committed template
.env
.env.*
!.env.example
credentials.json
.DS_Store
*.log
.turbo

# maintainer-local project log (never committed)
ISA.md

# scaffolding byproducts (safe to delete from disk)
.tmp/
docs/superpowers/
${HOME}/
