# STOP — do not put keys here.
#
# Expo loads env from the app directory (where app.json lives), not the repo root.
# Canonical template + instructions:
#   apps/mobile/.env.example
# Your real keys (gitignored):
#   apps/mobile/.env.local
#
#   cp apps/mobile/.env.example apps/mobile/.env.local
